// Curved Card Row 3D Tilt Effect\ndocument.querySelectorAll('.curved-card').forEach(card => {\n  card.addEventListener('mousemove', (e) => {\n    const rect = card.getBoundingClientRect();\n    const x = e.clientX - rect.left;\n    const y = e.clientY - rect.top;\n    const centerX = rect.width / 2;\n    const centerY = rect.height / 2;\n    const rotateX = ((y - centerY) / centerY) * 10;\n    const rotateY = ((x - centerX) / centerX) * 10;\n    card.style.transform = `translateY(-18px) scale(1.08) rotate(calc(var(--card-index) * 10deg - 15deg)) rotateX(${-rotateX}deg) rotateY(${rotateY}deg)`;\n    card.style.zIndex = 4;\n  });\n  card.addEventListener('mouseleave', () => {\n    card.style.transform = `translateY(0) rotate(calc(var(--card-index) * 10deg - 15deg)) scale(1.04)`;\n    card.style.zIndex = 2;\n  });\n});\n
